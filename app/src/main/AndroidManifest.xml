<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HorusHealth">

        <activity
            android:name=".ui.activity.LoginActivity"
            android:theme="@style/Theme.HorusHealth.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.activity.RegisterSuccessActivity" />
        <activity android:name=".ui.activity.RegisterActivity" />
        <activity android:name=".ui.activity.MainActivity" />
        <activity android:name=".ui.activity.HistorialActivity" />
        <activity android:name=".ui.activity.Step1Activity" />
        <activity android:name=".ui.activity.Step2Activity" />
        <activity android:name=".ui.activity.Step2aActivity" />
        <activity android:name=".ui.activity.Step2bActivity" />
        <activity android:name=".ui.activity.Step3Activity" />
        <activity android:name=".ui.activity.Step4Activity" />
        <activity android:name=".ui.activity.Step5Activity" />
        <activity android:name=".ui.activity.PerfilActivity" />

    </application>

</manifest>